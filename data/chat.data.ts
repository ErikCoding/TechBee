// Chat conversations are no longer seeded from static mock data —
// see services/chat.service.ts, which creates real conversations
// between whichever two accounts start messaging each other
// (persisted to Firestore once configured, localStorage until then).
export {}
