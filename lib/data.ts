// ─────────────────────────────────────────────────────────────
// DEPRECATED — kept only so the file isn't dangling.
//
// Mock data now lives in `data/*.data.ts`, domain types in
// `lib/types.ts`, and all reads should go through `services/*.service.ts`
// (the layer that will be swapped for Firebase later). Nothing in
// the app imports from this file anymore; it is safe to delete.
// ─────────────────────────────────────────────────────────────

export {}
